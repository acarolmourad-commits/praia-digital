# Template de Proposta Comercial — Hermes Agents

> Copie, adapte e use como base. Campos entre `{}` são variáveis.

---

## Cabeçalho

- **Proposta nº:** `{NUMERO}`
- **Data:** `{DATA}`
- **Validade:** `{VALIDADE}`
- **Cliente:** `{CLIENTE}`
- **Segmento:** `{SEGMENTO}`
- **Responsável:** `{RESPONSAVEL}`

---

## 1. Contexto e Diagnóstico Rápido

> 3-5 bullets traduzindo a dor em resultados mensuráveis.

- Hoje o time gasta ~`{HORAS_SEMANA}` horas/semana em `{TAREFA_REPETITIVA}`
- `{INDICADOR_1}` está em `{VALOR_ATUAL}` e a meta é `{VALOR_META}`
- `{GANHO_POTENCIAL}` aparece como oportunidade clara
- `{RISCO_ATUAL}` se mantido sem mudança

---

## 2. Proposta de Valor

> Uma frase única + 3 pilares.

**Frase única:**
“Hermes Agent para `{CLIENTE}`: autonomia supervisionada em `{CANAL_PRINCIPAL}`, com governança, LGPD e handoff humano.”

**Pilares:**

1. **Produtividade:** `{CANAL}` operando 24h com `{TAREFA_1}`, `{TAREFA_2}` e `{TAREFA_3}` automatizados.
2. **Previsibilidade:** métricas semanais + `{KPI_PRINCIPAL}` visível em dashboard.
3. **Controle:** aprovação humana para ações sensíveis + trilha de auditoria completa.

---

## 3. Escopo do Piloto

| Item | Descrição | Prazo | Critério de sucesso |
|------|-----------|-------|---------------------|
| `{FLUXO_1}` | `{DESCRICAO_1}` | `{PRAZO_1}` | `{METRICA_1}` |
| `{FLUXO_2}` | `{DESCRICAO_2}` | `{PRAZO_2}` | `{METRICA_2}` |
| `{FLUXO_3}` | `{DESCRICAO_3}` | `{PRAZO_3}` | `{METRICA_3}` |

**Fora do escopo (importante declarar):**
- `{ITEM_FORA_1}`
- `{ITEM_FORA_2}`

---

## 4. Investimento

| Modelo | Descrição | Valor |
|--------|-----------|-------|
| Piloto | `{PRAZO_PILOTO}` + setup | `{VALOR_PILOTO}` |
| Mensal | `{QTD_AGENTES}` agente(s) + suporte | `{VALOR_MENSAL}` |
| Anual | Desconto de `{DESCONTO}%` | `{VALOR_ANUAL}` |

**Forma de pagamento:** `{FORMA_PAGAMENTO}`
**Início:** `{DATA_INICIO}`

---

## 5. Próximos Passos

1. **Reunião de alinhamento:** `{DATA_REUNIAO}`
2. **Kick-off do piloto:** `{DATA_KICKOFF}`
3. **Primeira entrega:** `{DATA_ENTREGA_1}`
4. **Review e decisão:** `{DATA_REVIEW}`

**Aprovação:**
- [ ] Proposta aprovada
- **Nome:** _______________________
- **Data:** _______________________

---

## 6. Garantias e Governança

- `{PRAZO_GARANTIA}` de garantia de funcionamento do piloto
- SLA de resposta: `{SLA}`
- Dados armazenados em `{REGIAO}`
- Acesso restrito por `{MECANISMO_CONTROLE}`
- Cancelamento com `{PRAZO_CANCELAMENTO}` dias de aviso

---

## Anexos

- Anexo A: arquitetura do agente
- Anexo B: modelo de dados
- Anexo C: política de segurança e LGPD
- Anexo D: cronograma detalhado
