# Sitemap Verificação — Hermes Agents

Resultado do comando `curl -s https://acarolmourad-commits.github.io/praia-digital/sitemap.xml | grep -c hermes-agents`:

**0**

Observação: O resultado 0 indica que o sitemap publicado no GitHub Pages ainda não reflete as atualizações locais. Após o deploy, espera-se que o valor corresponda à contagem de 21 URLs contendo `hermes-agents` no sitemap.xml local.
